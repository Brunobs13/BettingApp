service:
  name: betting-app-engine
  host: 0.0.0.0
  port: 8080

runtime:
  log_path: artifacts/events.jsonl
  random_seed: 42

security:
  use_env_configuration: true
  hardcoded_secrets_allowed: false
